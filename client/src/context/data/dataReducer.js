import {
  //GET_ITEMS
} from "../actionTypes";

export default (state, action) => {
  switch (action.type) {
    // case GET_ITEMS:
    //   return {
    //     ...state,
    //     items: action.payload,
    //     loading: false
    //   };
    default:
      return state;
  }
};
